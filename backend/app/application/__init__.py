"""Application layer.

Use cases that orchestrate domain entities and ports to fulfil a single piece of
application behaviour (e.g. StartScanUseCase). Depends only on ``app.domain``.

Populated in Milestone 3.
"""
