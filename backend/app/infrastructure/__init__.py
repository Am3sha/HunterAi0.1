"""Infrastructure layer.

Concrete implementations of domain ports: the Tool Management subsystem
(Milestone 2), persistence/repositories (Milestone 4), and subprocess execution.
Depends on ``app.domain`` and ``app.application`` — never the reverse.
"""
