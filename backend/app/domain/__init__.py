"""Domain layer.

Pure business model: entities and ports (abstract interfaces). This package must
not import any framework, database, or I/O code. Everything here could run with
the Python standard library alone.

Populated in Milestone 3.
"""
